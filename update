#!/bin/bash
set -x
script_dir=$( cd "$(dirname "${BASH_SOURCE[0]}")" ; pwd -P )
stow --adopt --target="$HOME" home
sudo stow --adopt --target="/usr/bin" executables
chsh -s /bin/zsh
sudo chsh -s /bin/zsh
